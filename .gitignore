# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

/node_modules
# package lock is still too inconsistent with updates on install/update, just ignoring and relying soley on package.json
/package-lock.json
